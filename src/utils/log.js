export const logInfo = (text) => {
  // eslint-disable-next-line no-console
  console.log(text);
};

export const logError = (text) => {
  // eslint-disable-next-line no-console
  console.error(text);
};
